name: data
description: Data layer for restaurant app
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.0

# Add regular dependencies here.
dependencies:
  dio: ^5.8.0+1
  domain:
    path: ../domain
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  logger: ^2.5.0
  retrofit: ^4.4.2
  # path: ^1.8.0

dev_dependencies:
  build_runner: ^2.4.15
  freezed: ^3.0.6
  json_serializable: ^6.9.5
  lints: ^5.0.0
  retrofit_generator: ^9.2.0
  test: ^1.24.0
